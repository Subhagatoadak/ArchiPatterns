version: "3.9"
services:
  mobile-bff:
    build: .
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8101:8000"
    env_file:
      - .env.example
    volumes:
      - .:/app
