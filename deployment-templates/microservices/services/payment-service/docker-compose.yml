version: "3.9"
services:
  payment-service:
    build: .
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8003:8000"
    env_file:
      - .env.example
    volumes:
      - .:/app
