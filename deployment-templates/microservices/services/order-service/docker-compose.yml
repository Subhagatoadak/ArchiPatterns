version: "3.9"
services:
  order-service:
    build: .
    command: uvicorn src.main:app --host 0.0.0.0 --port 8000 --reload
    ports:
      - "8002:8000"
    env_file:
      - .env.example
    volumes:
      - .:/app
